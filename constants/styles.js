import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  blockConatiner: {
    backgroundColor: "#1c1c1c", // Dark background color
    borderRadius: 16, // Rounded corners as seen in the image
    padding: 20,
    marginVertical: 8, // Vertical spacing between containers
    marginHorizontal: 16,
    shadowOpacity: 0.25,
    shadowRadius: 3.84,
    elevation: 2,
  },
});

export default styles;
